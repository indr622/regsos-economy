<%= form_with(model: area) do |form| %>
  <% if area.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
        <% area.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name,description, style: "display: block" %>
    <%= form.text_area :name,description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
